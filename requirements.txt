# This is a comprehensive requirements.txt file for all labs to ensure that one installation covers everything.

# Do not include azure-functions-worker in this file
# The Python Worker is managed by the Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

requests
jsonref
python-dotenv
pandas
matplotlib
redis
msal
openai
openai[realtime]
azure-identity
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-openai-v2
azure-monitor-opentelemetry
azure-monitor-ingestion
azure-ai-projects
azure-ai-agents
azure-ai-inference[prompts]
azure-search-documents
azure-functions
azure-mgmt-apimanagement

# Pin pydantic to 2.12.x: semantic-kernel declares <2.12 but pydantic-core 2.33.x
# (used by pydantic 2.11) has no Python 3.14 wheels and fails to compile.
# Pydantic 2.12.x uses pydantic-core 2.41.x which ships Python 3.14 wheels.
# Install with: uv pip install -r requirements.txt --override requirements.txt --prerelease=allow
pydantic>=2.12,<2.13

# MCP Labs
mcp>=1.24.0
autogen-core 
autogen-ext[openai,azure,mcp] 
autogen-agentchat
semantic-kernel[mcp]

# Agent Framework
agent-framework